# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cybersec_platform?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&autoReconnect=true
  kafka:
    consumer:
      group-id: cybersec-dev

logging:
  level:
    com.cybersec.domain.mapper: DEBUG
    org.mybatis: DEBUG
